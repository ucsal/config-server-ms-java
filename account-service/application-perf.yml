server:
  port: 8080
spring:
  application:
    name: account-service

management:
  endpoints:
    web:
      exposure:
        include: "*"

account-service:
  greeting: "PEGOU"
  default-value: "Configuração Perf"


